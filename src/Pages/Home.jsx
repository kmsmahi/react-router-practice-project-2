import React from 'react';
import Navbar from '../Components/Navbar/Navbar';
import { Outlet } from 'react-router';

const Home = () => {
    return (
        <Navbar></Navbar>
        

    );
};

export default Home;